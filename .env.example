# Network（三仓库统一键名：只填裸 IP 与端口，其余自动组装）
# Vite dev proxy 目标：Ctrl 服务的地址（默认 127.0.0.1:5000，WSL2 下 Windows 直连 WSL 可用）
CTRL_IP=127.0.0.1
CTRL_PORT=5000

# Vite 自身端口（默认 5173，无需改动可留空）
# WEB_PORT=5173

# HTTPS 开关（默认 true；http 由部署层 301 重定向到 https）
ENABLE_SSL=true
